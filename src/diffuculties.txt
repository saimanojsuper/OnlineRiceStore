Try Changing map(() => {}) to map(() => ())

{} - Creates a code block that expects an explicit return statement.
With () - implicit return takes place.



Working To Do:
. Converting state management into redux
. Footer position to be corrected for all fields
. cart cards positioning must be corrected
. using api instead of local json
. adding login functionality if needed.
